apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "brain.fullname" . }}-test-connection"
  labels:
    {{- include "brain.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "brain.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
