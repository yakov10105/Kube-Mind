apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kube-mind-observer.serviceAccountName" . }}
  labels:
    {{- include "kube-mind-observer.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: {{ .Values.serviceAccount.automount }}